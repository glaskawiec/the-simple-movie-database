import React from 'react';
import PropTypes from 'prop-types';
import LogoTextStyled from './LogoTextStyled';

const Logo = props => (
  <LogoTextStyled>TSMBD</LogoTextStyled>
);

Logo.propTypes = {

};

export default Logo;
