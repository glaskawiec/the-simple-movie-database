import { GLOBAL_THEME_CHANGE } from '../ActionTypes/global';

// eslint-disable-next-line import/prefer-default-export
export const globalThemeChange = theme => ({ type: GLOBAL_THEME_CHANGE, theme });
