import styled from 'styled-components';

const Role = styled.p`
padding: 0 10px;
    font-size: 0.9em;
margin-bottom: 0;
    line-height: 1.4em;
    overflow: hidden;
    text-overflow: ellipsis;
`;

export default Role;
