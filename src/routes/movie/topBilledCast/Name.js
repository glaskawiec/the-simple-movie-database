import styled from 'styled-components';

const Name = styled.p`
    padding: 10px 10px 0 10px;
  font-weight: bold;
  font-size: 1em;
  margin-bottom: 0.3em;
    line-height: 1.4em;
    overflow: hidden;
    text-overflow: ellipsis;
`;

export default Name;
