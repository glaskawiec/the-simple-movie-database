import styled from 'styled-components';

const Name = styled.p`
  font-size: 1em;
  line-height: 1.4em;
  margin: 0;
  padding: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  font-weight: bold;
`;

export default Name;
